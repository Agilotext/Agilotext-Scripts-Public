Lets say you're working on a project in cloud code. You have just opened a new session and you want to understand the code base before building something new. So you ask a question. What does browser use do? Give me one line summary of each major component. It's a very simple question. And now watch what happens before it answers a single word, it reads the project readme to figure out what kind of project this even is. It goes through multiple files. It builds a mental model from scratch. Reading through your code base file by file, and every one of those reads costs money. And the answer you get is only as good as what Claude managed to find in those reads. Now, here's the issue. If you close that session and open a new one tomorrow, Claude starts from zero again. No memory of what it read, no map of how the files are connected. Every session it rebuilds the same understanding from scratch. It burns the same tokens, makes the same searches, rereads the same files every time. There is a tool that fixes this. It's called Graphify. It reads your project once, builds a knowledge graph of how everything connects, and cloud reads that graph at the start of every session instead of re reading your files. Let me show you how this works, and then I'll show you the actual numbers from running ten questions in two identical sessions. Here's the simplest way to understand what just happened. So you can think of a cloud session as a new hire that shows up on their first day. They have never seen the project and they have to read through everything before answering a single question. And every new session means a new hire. Graphify builds the senior colleague, someone who already has the map, who already knows where the decision logic lives, which components talk to each other, and which five components are the hub that everything routes through. You build that map once, and Claude reads it at the start of every session automatically. And that's the whole idea. And it's not just for code. It also works for research papers, meeting recordings, strategy documents, or any kind of mixed content. I'll come back to that, but first let me show you exactly how it works under the hood. This tool shipped forty eight hours after Andre Karpathy, co founder of OpenAI, described exactly what it should do. Currently, it has almost twenty five k stars, so there are three passes, and that's the whole system. First pass analyzes the code structure. If your folder has Python, typescript, Go, Rust, or anything with real syntax, Graphify runs a code parser across every file. It reads every class, every function, every import, every call. This runs entirely on Your machine, so no external API calls are made and no tokens are used. It extracts hard facts like this function, calls that one, this class, imports this module, and stuff like that. Second pass analyzes the audio and video. If you've got meeting recordings, tutorials or YouTube URL's, Graphify transcribes them locally using Faster Whisper, which is also free. In the third pass, it scans everything else, like markdown, PDF's, images, readme files, docs, etcetera. Claude subagents run in parallel, extract concepts and relationships and figure out what things mean and how they connect with each other. Pass three is the only part that touches Claude's API and it only runs once. But after that, every session reads the cached graph for free. Everything merges into one graph. An algorithm groups related concepts into neighborhoods. You can think of them as departments. Every time you open a new conversation or session, before Claude reads a single file, that hook fires and Claude reads a summary of the entire graph. Then it makes two or three targeted reads instead of fifty fifteen and that's where the savings come from. Now, does this actually save tokens? I ran ten questions in two identical sessions to find out. It's the same project, same ten questions, asked in the same order. The session on the left hand side is not using Graphify, the one on the right is using Graphify. Let me run all of these ten questions. Now, if we run context in both of these sessions, the session without Graphify is using one hundred twenty k tokens and the one using Graphify is using one hundred thirteen k tokens. So it's not a big difference, probably less than eight percent. However, if you look at the questions and their replies, I think the quality of responses in the session using Graphify is better than the one which is not. So if you look at this question two, both of these found that there are three phases. But on the left hand side it simply mentions these three functions as ah, phases. But on the right hand side it has explained this phase one in detail and similarly phase two and phase three it has also added details about loop detection and replanning, etcetera. If you look at Graphify's GitHub repository, you will see these worked examples where they're claiming that they achieved seventy one point five x reduction. So why did our sessions look more like seven to eight percent reduction? I'll explain exactly what that benchmark actually measures and when the big savings are. But first let's see how to set it up. You can scroll down to the install section, then copy this command Go to your project, open a terminal, paste and run. If you are on Windows you will see this error. It's because double ampersand is not recognized. So you can simply run each of these commands separately. First let's run Pip Install Graphify. It's complete. Now let's run Graphify Install and this is now ready. And by the way, if you are using other platforms like codecs, opencode, etcetera. You can copy the corresponding command from here and replace this Graphify install with it. Once that is done, we want our Claude code to use Graphify in every session without us asking to do it. And to set it up we need to run this command Graphify Claude Install and it is done. Now we can start a new Claude session and run Graphify. So this is a new session. Now let's run Graphify dot. Alright, so it has found a corpus of three hundred fifty seven code files, fifty three docs and seven images. So I think there is a threshold of two hundred files in one go. So it's asking us to select subdirectories for which we want to create the graph. Let's give browser use and examples. Okay, so it took around twelve minutes to complete and here's what we get. There is a new directory called Graphify out. It contains graphreport md. It has identified four thousand forty one nodes, twenty thousand nine hundred edges and one hundred eighty five communities. We also get this graph HTML and if you open it in browser, this is what you are going to see. On the right hand side we have the list of communities that it has identified and by default all of these are selected. So you can deselect any of these by clicking on that community. Each dot in this graph is a concept, a class, a function, a documented idea, and each line is a relationship between two dots or two concepts. The colors are neighborhoods and the bigger the dot, the more things connect to it. Now, building this entire graph was really cheap and we just need to build it once and then Claude has access to this entire graph. It can easily understand how the things are connected so that it can selectively scan those relevant docs instead of blindly going through a large number of docs. Now, when you're working on your project, you're of course making changes, adding new files, making code changes, etcetera. And you want to keep that graph in sync. Now to do that there are a couple of options. One is you can simply run Graphify Update and it will automatically identify what changed from the previous run and update your graph. Or you can run Graphify hook install and this will wire up git hooks. The graph rebuilds automatically if there is a new commit or when you switch your branch and it has been set up successfully. Now here's what this actually means for for your day to day Every time you open a new Claude session on any project where you have run Graphify, that graph report gets read first automatically. You don't type anything special, you don't run a command, you just ask a question and Claude already has the map the seventy one. X claim. Here's what it actually measures. The benchmark took a uh, fifty two file corpus, three repos, five papers and four images. The naive approach loading all fifty two files directly into Claude's context at once that costs around one hundred twenty three thousand tokens. Whereas Graphify's approach is to first read that graph file and only go through the relevant neighborhoods in the graph. And it consumed only seventeen hundred tokens per query and that ratio is seventy one point five. X. But here's the thing, nobody works by pasting fifty two files into your Claude conversation. In real Claude code sessions, you will probably tag your folder and stack, start asking questions. You will never tag a large number of files because you wouldn't know where to look at. So that seventy one. X benchmark is comparing against a workflow most people don't actually use. So this number actually doesn't make any sense. Your savings will be considerable if you're working on mixed projects that contains code plus docs, or if you are working on long sessions or if you have large projects. If your project has more files, Claude would otherwise search through more number of files, but with the help of Graph it can reduce that search. So what Graphify actually gives you in cloud code is fewer wrong direction file reads, better answers from the first message, and savings that grow with session length. This isn't only for code projects. I ran Graphify on a folder of YouTube scripts, transcripts and research notes. There was no Python or JavaScript, just markdown and text files. And I asked questions like what topics I have covered most and what's missing in my content on this subject, etcetera. And it answered using the graph, not by reading every file. And it's the same idea for research folders. You can point it to a stack of PDF's or papers and Claude can start making connections across all of them. You can have just one connected map for all of your meeting recordings, strategy docs and client files. And the short version is if Claude keeps reading the same things over and over in your sessions. Graphify is worth running, so if you are using this for research vault or business planning folder, it's definitely worth it. I hope you found the video useful and I would love to know your thoughts and experience on this. Don't forget to like the video and subscribe the channel and I'll see you in the next one.