Anon2 Prod 1.09 Test Notes

Fichiers ajoutés
- anonymisation/agiloshield-embed-anonymisation-anon2-beta.js
- anonymisation/index-anon2-beta.html
- Front_END/FINAL-2026-05-10-anon2-beta.html
- anonymisation/fixtures/inclusion-exclusion-smoke.txt
- anonymisation/WEBFLOW-DEPLOY-INCLUSION-2.4.15.md

Endpoints Anon2 branchés
- /anon2AsyncOfficeText
- /getAnon2JobsInfo
- /getAnon2Status
- /receiveAnon2Text
- /receiveAnon2Zip
- /deleteAnon2Job
- /getAnon2UserDefaults
- /setAnon2UserDefaults
- /getAnon2UserOptions (fallback temporaire uniquement si endpoint Defaults absent: 404/405/501/no route)
- /setAnon2UserOptions (fallback temporaire uniquement si endpoint Defaults absent: 404/405/501/no route)
- /reconcileAnon2Text
- /getAnon2Usage (attendu backend — compteur crédits, v2.4+ front)
- /getVersion (gate Inclusion/Exclusion >= 4.0.79)

Monétisation (mai 2026)
- edition API: agiloshield (plus de mapping anonymisation -> free)
- Quotas documentés: free 10/j, payant 250/j (fallback UI si usage API absent)

URLs de test
- Page locale: anonymisation/index-anon2-beta.html
- Snippet Webflow: Front_END/FINAL-2026-05-10-anon2-beta.html
- Page canary prod: /app/business/dashboard/anonymiser
- Script CDN 2.4.15 (pin SHA commit, pas @1.09) :
  - https://cdn.jsdelivr.net/gh/Agilotext/Agilotext-Scripts-Public@5e9fb55cdbd212240a6440b1c05f8d9916774d5a/CNOEC_Agiloshield_Docs/anonymisation/agiloshield-embed-anonymisation-anon2-beta.js?v=2.4.15

Vérifications déjà faites
- Syntaxe JS validée avec node --check
- Endpoints Anon2 bien substitués dans le bundle
- Chargement/sauvegarde des options Anon2 avec fallback Defaults -> UserOptions strictement limité à l’absence d’endpoint
- Upload async : username + token + edition + removeImages + fileUpload[]
- Depuis 2.4.15 : upload envoie aussi anon2InclusionListJson + anon2ExclusionListJson (JSON arrays) si getVersion >= 4.0.79 et feature inclusion active
- Anciens champs legacy includeTerms/excludeTerms retirés du flux fichier et du collage texte (/anonText)
- Liste officielle Anon2 exposée côté UI: ADR, DAT, EML, IBA, IDN, JOB, LOC, ORG, PER, PII, PRO, TEL, URL
- Restauration Anon2 branchée côté frontend avec validation ZIP multi-fichiers par signature PK
- receiveAnon2Zip validé par signature PK avant téléchargement
- UI Inclusion/Exclusion : canary Business + gate API ; listes en localStorage navigateur

Smoke API prod (10/08/2026, lecture seule, sans toucher kawansoft)
- getVersion = Agilotext API - 4.0.98 - 07-Aug-2026
- Fixture inclusion-exclusion-smoke.txt → job 1000037769
- ACME_FORCE_ANON_TEST → <PII_AA> (inclusion OK)
- KEEP_VISIBLE_TEST reste visible (exclusion OK)

Checklist de validation manuelle
- Charger le snippet / pin CDN 2.4.15 sur Business anonymiser
- Console : __AGILO_EMBED_ANON_VERSION__ === '2.4.15'
- Bouton Inclusion/Exclusion visible sans ?featureInclusionUi=1 sur /business/
- Ajouter termes, Save, reload → listes encore là (localStorage)
- Network upload : FormData contient anon2InclusionListJson et anon2ExclusionListJson (même [])
- Inclusion seule : terme source anonymisé / tagué
- Exclusion seule : terme préservé
- Conflit inclusion+exclusion : exclusion gagne
- Terme < 3 caractères refusé côté UI
- ?featureInclusion=0 : pas d’envoi listes
- Upload sans listes : pas de régression
- Collage texte : toast « s’applique aux fichiers », pas de includeTerms/excludeTerms
- Vérifier que le payload upload ne contient pas processingMode/pseudoStrategy/pseudoScope/pseudoKeyMode/pseudoRestoreWindow/pseudoDeterministic/pseudoPreserveFormat/includeTerms/excludeTerms
- Tester upload PDF / DOCX / lot multi-fichiers
- Vérifier polling PENDING -> READY + téléchargements ANON / ZIP / .properties
- Vérifier restauration + historique + suppression job
